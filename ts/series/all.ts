import {
  AREA as AreaValue,
  BAR as BarValue,
  BASELINE as BaselineValue,
  CANDLESTICK as CandlestickValue,
  HISTOGRAM as HistogramValue,
  LINE as LineValue,
} from "./index";

export enum SERIES {
  AREA = AreaValue,
  BAR = BarValue,
  BASELINE = BaselineValue,
  CANDLESTICK = CandlestickValue,
  HISTOGRAM = HistogramValue,
  LINE = LineValue,
}
