import {
  CENTER as CenterValue,
  LEFT as LeftValue,
  RIGHT as RightValue,
} from "./index";

export enum HORZ_ALIGN {
  LEFT = LeftValue,
  RIGHT = RightValue,
  CENTER = CenterValue,
}

export const enum VERT_ALIGN {
  LEFT = LeftValue,
  RIGHT = RightValue,
  CENTER = CenterValue,
}
