import {
  TOP as TopValue,
  RIGHT as RightValue,
  CENTER as CenterValue,
  BOTTOM as BottomValue,
  LEFT as LeftValue,
} from "./index";

export enum HORZ_ALIGN {
  LEFT = LeftValue,
  RIGHT = RightValue,
  CENTER = CenterValue,
}

export const enum VERT_ALIGN {
  TOP = TopValue,
  BOTTOM = BottomValue,
  CENTER = CenterValue,
}
